# The pipeline qits-ci runs on every push to a tracked branch of this repository.
#
# One ordinary sandboxed step, and no image: this repository does not ship a container. The app is
# packaged and served by qits-gateway, which carries this repo as a git submodule at its Quinoa
# ui-dir and builds it into the gateway image — so this pipeline's job is to keep main green
# (install, lint, test, build), and the gateway's pipeline is where the bundle becomes deployable.
#
# The second step is the release train's maintenance leg, and it is bound to maintenance/* branches.
# On every other push it is recorded SKIPPED and changes nothing. "Release only after green tests"
# needs no machinery: a failing step stops the loop, so the leg below is simply never reached.
steps:
  - image: qits/build-images/node-base:latest
    timeout-seconds: 1800
    script: |
      # The daemon runs this with `bash -c` and no -e, so without this an early failure would be
      # swallowed by the next command's exit code. -u is load-bearing too: an unset registry var
      # must stop the step, not quietly resolve the @qits scope against npmjs.org, where these
      # packages do not exist and never will.
      set -eu

      # THE LOCKFILE is the one file this step edits, and npm leaves no alternative.
      #
      # npm pins a full resolved URL per package, and this lockfile is generated on the deployment
      # host, so every entry names localhost:8081 — an address that does not exist in this
      # container. `npm ci` fetches tarballs by that URL and never asks the configured registry for
      # them, so the environment below does not reach them: without this swap the install dies on
      # connection refused. The paths are identical on both addresses, so replacing the ORIGIN and
      # keeping the path is all it takes, and the integrity hashes are what make that safe — bytes
      # from the other address must still hash the same. The clone is this step's own; nothing is
      # edited that anyone will ever read again.
      #
      # npm's own knob for exactly this, `--replace-registry-host=always`, cannot do it against a
      # registry mounted under a path. @npmcli/arborist glues rather than resolves (reify.js:
      # `registry.slice(0, -1) + resolvedURL.pathname`), so
      # http://qits-artifacts:8080/artifacts/npm/npmjs/ + /artifacts/npm/npmjs/zone.js/-/…tgz comes
      # out as …/artifacts/npm/npmjs/artifacts/npm/npmjs/zone.js/-/…tgz and every tarball 404s.
      # Measured on npm 10.9.4.
      #
      # The origin is derived from the injected env rather than spelled here: no repository states
      # a deployment's addresses, and the one address this repo does state (localhost:8081, in
      # .npmrc, for a developer on the host) is the one being replaced. Both registries are the
      # same qits-artifacts, so one origin covers the cached npmjs tarballs and the @qits ones
      # alike; were that ever to stop being true, the scoped tarballs would 404 rather than resolve
      # to something wrong.
      npm_origin=$(node -p 'new URL(process.env.QITS_NPM_PROXY_URL).origin')
      sed -i -E "s#(\"resolved\": \")https?://[^/\"]+#\1$npm_origin#" package-lock.json

      # ENVIRONMENT, not a file. This repository COMMITS an .npmrc (localhost:8081, right for a
      # developer on the host), and npm ranks a project .npmrc ABOVE ~/.npmrc — so the
      # `cat > ~/.npmrc` preamble the library repos use would be written and silently ignored.
      # `npm_config_*` environment outranks both files, so it wins without touching the tree.
      env npm_config_registry="$QITS_NPM_PROXY_URL" \
          "npm_config_@qits:registry=$QITS_NPM_REGISTRY_URL" \
          npm ci --no-audit --no-fund

      npm run lint

      # The jsdom target. This image has no browser and CI is not the place to download one.
      npm run test

      npm run build

  # THE MAINTENANCE LEG. .config/qits/ci-event-upstream-ui-components.yml bumps a released
  # dependency and force-pushes the result to maintenance/<upstream>; this step is what turns that
  # branch into a release of this repository, and so into the next SoftwareRelease on the train.
  #
  # It runs after the tests above, in the same run, on the same push. That ordering IS the "only
  # release if the tests passed" rule — no coupling machinery, no second pipeline, one run per push.
  - image: qits/build-images/node-base:latest
    branches:
      - prefix: maintenance/
    timeout-seconds: 300
    script: |
      set -eu

      # DEFENCE IN DEPTH against an older or rolled-back qits-ci. An engine that does not know the
      # `branches:` key above ignores it and runs this step on EVERY push — feature branches
      # included — so the branch is asserted here as well. Free when the filter works, and a loud
      # red step instead of an accidental release when it does not.
      case "$QITS_CI_BRANCH" in
        maintenance/*) ;;
        *) echo "not a maintenance branch: $QITS_CI_BRANCH"; exit 1 ;;
      esac

      # The bump commit's subject, reused verbatim as the release summary. It is under the summary
      # field's 100 characters by construction — the bump pipeline's convention is what keeps it so.
      summary=$(git log -1 --format=%s)

      # RELEASE, not merge and not integrate. Releasing is the only door onto main: it stamps a
      # version, writes one two-parent commit and publishes SoftwareRelease. Integrating targets the
      # branch's PARENT and would refuse with RELEASE_REQUIRED when that parent is main.
      #
      # The step self-identifies entirely from injected environment, so no deployment address is
      # spelled here: $QITS_WORKSPACES_URL is qits-workspaces' root (scheme, host and port, no path
      # — the path is the caller's to spell), $QITS_CI_REPO_ID and $QITS_CI_BRANCH are this run's.
      #
      # The POST goes through node because node-base carries no jq and promises no curl (measured);
      # node has fetch built in and needs nothing installed. Statuses: 200 is the receipt and prints
      # it, a 409 saying the branch is already released is a replayed push finding its work done and
      # is a success, and everything else is red with the body on screen. Note the body is read
      # defensively — the "cannot release main into itself" refusal is a plain 400 with no `reason`.
      node -e '
        const [url, branch, summary] = process.argv.slice(1);
        fetch(url, {
          method: "POST",
          headers: { "content-type": "application/json" },
          body: JSON.stringify({ branch, summary }),
        })
          .then(async (res) => {
            const text = await res.text();
            let body = {};
            try {
              body = JSON.parse(text);
            } catch {
              /* not JSON: the raw text is what gets printed below */
            }
            if (res.ok) {
              console.log(`released ${body.version} as ${body.commitSha} from ${body.branch}`);
              return;
            }
            if (res.status === 409 && body.reason === "ALREADY_INTEGRATED") {
              console.log(`${branch} is already released - nothing to do`);
              return;
            }
            console.error(`release refused: HTTP ${res.status} ${text}`);
            process.exit(1);
          })
          .catch((err) => {
            console.error(`release call failed: ${err.message}`);
            process.exit(1);
          });
      ' "$QITS_WORKSPACES_URL/workspaces/api/branches/release?repositoryId=$QITS_CI_REPO_ID" \
        "$QITS_CI_BRANCH" "$summary"
